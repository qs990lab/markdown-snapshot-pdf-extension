# コードブロック内の$記号テスト

以下のコードにはドル記号が含まれています：

```bash
echo "Price: $100"
export PATH=$PATH:/usr/local/bin
```

```javascript
const price = "$50";
console.log(`Total: ${price}`);
```

これは数式です：$E = mc^2$

```python
def calculate_price(base_price):
    return f"${base_price * 1.1:.2f}"
```

もう一つの数式：$$\int_{0}^{\infty} e^{-x} dx = 1$$
